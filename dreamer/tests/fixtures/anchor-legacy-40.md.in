# Anchor gate legacy-hash fixture

## Question

Do legacy full-length hashes pass the short-anchor gate?

## Answer

No. Both rows deliberately use the retired 40-character grammar.

## Derivation trail

The test materializes live full-length commit and object hashes and requires a grammar rejection.

Provenance: {{TODAY}} · sid deadbeef

## Anchors

- `{{PATH_ONE}}` — {{TYPE_ONE}} `{{HASH_ONE}}`
- `{{PATH_TWO}}` — {{TYPE_TWO}} `{{HASH_TWO}}`
