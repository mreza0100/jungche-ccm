Use the Workflow tool exactly once.

Invoke this bundle:
{{SCRIPT_PATH}}

Pass this exact JSON value as the Workflow `args` value:
{{ARGS_JSON}}

Wait for the background workflow to finish, then return its result without redesigning, repairing, or re-running the workflow.
