[project]
name = "pfm-harvest-browser"
version = "0.1.0"
description = "Opt-in real-browser rung (Patchright + system Chrome) for the harvest wall-bypass ladder."
requires-python = "==3.11.*"
dependencies = [
    "patchright==1.62.1",
]

[tool.uv]
package = false
