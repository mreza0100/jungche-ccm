---
name: chat:group:ls
description: List chat groups on the group bus — members, message counts, this chat's unread backlog — via `pfm chat group ls`. Trigger — /chat:group:ls.
argument-hint: (no arguments)
---

# Chat Group LS

Run `$HOME/.local/bin/pfm chat group ls` and report the groups, flagging the ones this chat belongs to and any unread backlog.
