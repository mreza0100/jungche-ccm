---
name: chat:group:ls
description: List chat groups on the group bus — members, message counts, this chat's unread backlog — via $HOME/.claude/commands/chat/group.sh ls. Trigger — /chat:group:ls.
argument-hint: (no arguments)
---

# Chat Group LS

Run `$HOME/.claude/commands/chat/group.sh ls` and report the groups, flagging the ones this chat belongs to and any unread backlog.
