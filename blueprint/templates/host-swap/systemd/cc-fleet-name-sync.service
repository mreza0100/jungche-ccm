[Unit]
Description=Converge every live chat's tmux window name (cc-fleet name-sync)

[Service]
Type=oneshot
# The engine, not a shell satellite: window naming has exactly one writer, and
# it is the same gather pass the picker runs.
ExecStart=%h/.local/bin/cc-fleet name-sync
