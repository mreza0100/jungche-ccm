---
name: p:tokens
description: Compatibility alias for token attribution requested by the pinned legacy Wave Walker. Use /tokens for new requests.
---

Load and execute `/tokens` with the caller's arguments. Its source is `../tokens/SKILL.md` relative to this command directory.
